machine:
  environment:
    AWS_REGION: 'ap-northeast-1'

test:
  override:
    - mvn test:
        timeout: 1200
  post:
    - mkdir $CIRCLE_ARTIFACTS/plugin
    - mv target/* $CIRCLE_ARTIFACTS/plugin

